<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:security="http://www.springframework.org/schema/security"
       xsi:schemaLocation="
            http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
            http://www.springframework.org/schema/security http://www.springframework.org/schema/security/spring-security.xsd
       ">
    <security:http auto-config="false" use-expressions="true">

        <security:form-login
                login-page="/auth/login.do"
                login-processing-url="/login"
                authentication-success-handler-ref="customLoginSuccess"
                authentication-failure-url="/auth/login.do?error=true"
        />
        <security:logout
                logout-url="/login/logout.do"
                logout-success-url="/auth/login.do?logout=true"
                invalidate-session="true"
                delete-cookies="remember-me,JSESSION_ID" />

        <security:intercept-url pattern="/resources/**" access="permitAll" /> <!-- 정적 리소스 모두 접근 허용 -->
        <security:intercept-url pattern="/auth/login.do" access="permitAll" />
        <security:intercept-url pattern="/" access="permitAll" />
        <security:intercept-url pattern="/index.do" access="permitAll" />
        <security:intercept-url pattern="/admin/**" access="hasRole('ROLE_ADMIN')" /> <!-- 관리자만 접근 -->
        <security:intercept-url pattern="/**" access="isAuthenticated()" /> <!-- 나머지는 인가된 접근만 허용 -->

        <security:remember-me data-source-ref="dataSource" token-validity-seconds="604800" />

        <security:csrf disabled="false"/>

    </security:http>

    <security:authentication-manager>
        <security:authentication-provider user-service-ref="customUserDetailService">
            <!-- 인코딩 -->
            <security:password-encoder ref="bcryptPasswordEncoder" />
        </security:authentication-provider>
    </security:authentication-manager>


    <!--빈(Bean) 설정-->
    <bean id="customUserDetailService" class="grpstudy.grpware.auth.security.CustomUserDetailService" />
    <bean id="customLoginSuccess" class="grpstudy.grpware.auth.security.CustomLoginSuccessHandler" />
    <bean id="bcryptPasswordEncoder" class="org.springframework.security.crypto.bcrypt.BCryptPasswordEncoder" />
    <bean id="customAccessDenied" class="grpstudy.grpware.auth.security.CustomAccessDeniedHandler" />
</beans>